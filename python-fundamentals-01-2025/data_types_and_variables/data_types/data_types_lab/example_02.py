# Original list
numbers = [1, 2, 3, 4, 5]

# Iterate over the list using its indices and modify elements in place
for i in range(len(numbers)):
    numbers[i] *= 2  # Double each element

# Print the modified list
print("Modified List:", numbers)

